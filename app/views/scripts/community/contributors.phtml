<?php echo $this->partial('community/sidebar') ?>
<?php echo $this->partial('community/header', array('active' => 'contributors')) ?>
<?php echo $this->dynamicHeader('subPageMainHeader', 'Contributors') ?>

<p class="first">
   Meet the people who have helped make Zend Framework the leading
   PHP 5 web application framework. 
</p>

<div class="dotted-line"></div>

<noscript>
<p>
    Due to the number of images on this page, we are using JavaScript to defer
    image loading until after the page has completed rendering. If you are 
    seeing this message, you likely have JavaScript disabled, and thus you will
    note that the images are not present.
</p>
</noscript>

<?php
$this->dojo()->addOnLoad('function() {
    dojo.query("img[data-defer-src]").forEach(function(node) {
        var src = dojo.attr(node, "data-defer-src");
        dojo.attr(node, "src", src);
        dojo.removeAttr(node, "data-defer-src");
    });
}');
echo $this->render('community/_contributors.phtml');
?>

<p>
    <em>If you are on this list and wish to use a different image, update the
    <a href="http://gravatar.com/">Gravatar</a> associated with the email with
    which you registered your CLA.</em>
</p>
