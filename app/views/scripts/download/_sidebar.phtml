<? $this->rightNav()->captureStart('append', 'Get Started') ?>
    <p>
        A great place to get up to speed quickly is the Zend Framework <a href="/manual/en/learning.quickstart.intro.html">QuickStart</a>.
    </p>
<? $this->rightNav()->captureEnd() ?>

<? $this->rightNav()->captureStart('append', 'Browse the Repository') ?>
    <p>
        You may browse the subversion repository online using the
        <a href="/code">Zend Framework code browser</a>.
        You can also use this tool to search the repository,
        view changelogs, and create custom snapshots based on specific revisions or
        branches of the project.
    </p>
<? $this->rightNav()->captureEnd() ?>

<? $this->rightNav()->captureStart('append', 'Subversion Access') ?>
    <p>
        Complete source code is available from the Zend Framework Subversion repository via anonymous checkout.
    </p>
    
    <p>
        Instructions for accessing our repository can be found in our <a href="/wiki/display/ZFDEV/Subversion+Standards">Subversion Standards</a>.
    </p>
    
    <p>
        Subversion commit privileges are granted on an as-needed basis. If you feel you need such privileges,
        please <a href="/contact">contact us</a>.
<? $this->rightNav()->captureEnd() ?>
    
