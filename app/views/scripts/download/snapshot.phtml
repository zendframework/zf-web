<?= $this->partial ( 'download/sidebar' ) ?>
<?= $this->partial('download/header', array('title'  => 'Downloads', 'active' => 'snapshots')) ?>

<?= $this->dynamicHeader('subPageMainHeader', 'Snapshots') ?>

<p class="first">
    Our snapshots are built with every commit to the Zend Framework repository.
</p>

<div class="dotted-line"></div>

<p>
    While a new snapshot is built with every relevant commit to the repository
    on our <a href="/build">continuous integration server</a>, only successful
    builds are made available here. You can therefore be confident that all unit tests
    have run and passed against these snapshots.
</p>

<p>
    Please keep in mind that passing all unit tests is evidence- not a
    guarantee- of quality. Unit tests may still be under development.
    These snapshots should not be used in production environments.
</p>

<p>
    If you intend to commit changes to the project, you should consider
    checking out the latest version of Zend Framework from our
    <a href="/wiki/display/ZFDEV/Subversion+Standards">Subversion repository</a>. If you would like to download the latest version
    of Zend Framework regardless of the status of unit tests, tarballs of any
    directory in the repository at any revision are available in our <a href="/code">code
    browser</a>.
</p>

<table width="100%" cellpadding="2" cellspacing="0" border="0" style="border-bottom: 1px solid black">
    <tr>
    <th style="border-bottom: 1px solid black">Snapshot</th>
    <th align="right" style="border-bottom: 1px solid black">English</th>
    <th align="right" style="border-bottom: 1px solid black">All&nbsp;Languages</th>
        </tr>
    <? foreach ($this->snapshots as $base => $file): ?>
        <tr>
        <td><?= $base ?></td>
            <td align="right">
        <? if($file['en']): ?>
        <a href="/snapshots/<?= $file['en']['tar.gz']['filename']; ?>">tar.gz</a>&nbsp;(<?= $file['en']['tar.gz']['filesize']; ?>MB)
        <br/>
        <a href="/snapshots/<?= $file['en']['zip']['filename']; ?>">zip</a>&nbsp;(<?= $file['en']['zip']['filesize']; ?>MB)
        <? endif ?>
        </td>
            <td align="right">
        <? if($file['all']): ?>
        <a href="/snapshots/<?= $file['all']['tar.gz']['filename']; ?>">tar.gz</a>&nbsp;(<?= $file['all']['tar.gz']['filesize']; ?>MB)
        <br/>
        <a href="/snapshots/<?= $file['all']['zip']['filename']; ?>">zip</a>&nbsp;(<?= $file['all']['zip']['filesize']; ?>MB)
        <? endif ?>
        </td>
        </tr>
    <?
    endforeach
    ?>
</table>

