<? $this->rightNav()->captureStart('append', 'Would you like to subscribe to one of these lists?') ?>
		<p>Instructions for subscribing to and unsubscribing from Zend Framework mailing
		lists can be found on the
		<a href="/wiki/display/ZFDEV/Contributing+to+Zend+Framework#ContributingtoZendFramework-Subscribetotheappropriatemailinglists">
		Community Wiki</a>.</p>
<? $this->rightNav()->captureEnd() ?>

<? $this->rightNav()->captureStart('append', 'Archived Lists') ?>
<? $listNames = ArchivesController::$nabbleIDsToNames; ?>
<?
$listURLs = array(
    ArchivesController::ALL => '/archives/all',
    ArchivesController::ANNOUNCEMENTS => '/archives/announcements',
    ArchivesController::AUTH => '/archives/auth',
    ArchivesController::CORE => '/archives/core',
    ArchivesController::DATABASE => '/archives/database',
    ArchivesController::DOCUMENTATION => '/archives/documentation',
    ArchivesController::GENERAL => '/archives/general',
    ArchivesController::GDATA => '/archives/gdata',
    ArchivesController::I18N => '/archives/i18n',
    ArchivesController::FORMATS => '/archives/formats',
    ArchivesController::MVC => '/archives/mvc',
    ArchivesController::SERVER => '/archives/server',
    ArchivesController::WEB_SERVICES => '/archives/web-services'
);                                
?>
        <ul class="nav">
            <?php 
                foreach($listNames as $id => $name) {
                    echo '<li>';
                    if($this->id == $id) {
                        echo $name;
                    } else {
                        echo "<a href=\"$listURLs[$id]\">" . $name . '</a>';
                    }
                    echo '</li>';
                }
            ?>
        </ul>
<? $this->rightNav()->captureEnd() ?>

