<? if ($this->message == 'Page not found'): ?><div class="error-404"><div class="error-404-inner"><? endif; ?>
<?= $this->dynamicHeader('subPageMainHeader', $this->message) ?>

<p class="first">
    <?php
        $notFoundText = array(
            "NO, Zend Framework is <strong>not</strong> to blame for this!",
            "We hear Symfony doesn't have this problem.",
            $this->text,
        );
        echo $notFoundText[array_rand($notFoundText)];
    ?>
</p>

<? if ($this->message == 'Page not found'): ?></div></div><? endif; ?>

<?
$config = Zend_Registry::get('config');
if ($config->mvc->showTrace):               ?>
<div class="dotted-line"></div>

<h2>Backtrace</h2>
<pre class="error">
<?= $this->exception->getMessage() ?>

<?= $this->exception->getTraceAsString() ?>
</pre>

<h2>Request</h2>
<pre class="error">
<?= var_export($this->request->getParams(), 1) ?>
</pre>
<? endif ?>
