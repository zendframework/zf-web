<?php 
$entry = $this->entry; 
$ds    = function($ts) use ($entry) {
    $tz   = $entry->getTimezone();
    $date = new \DateTime();
    $date->setTimezone(new \DateTimeZone($tz));
    $date->setTimestamp($ts);
    return $date->format('Y-m-d H:i:s');
};
$url      = $this->plugin('url');
$entryUrl = $url('phly-blog/entry', array('id' => $entry->getId()));
$title    = $this->escapeHtml($entry->getTitle());
?>
<aside class="two columns alpha entry meta">
    <p class="date"><?php echo $ds($entry->getCreated()) ?></p>
</aside>

<section class="nine columns omega">
    <h3 class="entry"><a href="<?php echo $entryUrl ?>"><?php echo $title ?></a></h3>

    <div class="entry body"><?php echo $entry->getBody() ?></div>

    <p><a href="<?php echo $entryUrl ?>">Continue reading...</a></p>
</section>

