<?php
$dojoDataConfig = sprintf('async: 1, dojoBlankHtmlUrl: \'%s/js/ZfSiteBlog/blank.html\',
    packages: [ {
        name: \'ZfSiteBlog\',
        location: location.pathname.replace(/\/[^/]+$/, \'\') + \'js/ZfSiteBlog\'
    } ]', 
    '/' . $this->baseUrl()
);
$headScript = $this->headScript();
$headScript->appendFile('//ajax.googleapis.com/ajax/libs/dojo/1.8.0/dojo/dojo.js', 'text/javascript', array(
    'data-dojo-config' => $dojoDataConfig,
));
$headScript->appendScript('require(["ZfSiteBlog/blog"]);');

$this->headLink()->appendStylesheet('/css/ZfSiteBlog/blog.css');
?>
