<?php
$this->headLink()->prependStylesheet('//ajax.googleapis.com/ajax/libs/dojo/1.8/dojox/highlight/resources/pygments/autumn.css');

$dojoDataConfig = 'async: true, parseOnLoad: true';
$this->placeholder('scripts')->append(sprintf(
    '<script data-dojo-config="%s" src="//ajax.googleapis.com/ajax/libs/dojo/1.8.0/dojo/dojo.js"></script>', 
    $dojoDataConfig
));

$this->inlineScript()->captureStart(); ?>
    // Require the Dialog class
    require(["dojox/highlight", "dojo/query", "dojox/highlight/languages/_www", "dojo/domReady!"], function(highlight, query){
        query("pre code.highlight").forEach(highlight.init);
    });
<?php $this->inlineScript()->captureEnd(); ?>
