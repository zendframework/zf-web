<?php 
$this->layout()->active = 'participate';
$this->render('page-controller/contact/sidebar');
$this->headTitle()->prepend('Contact'); 
?>
<section class="contact">
<h1>Contact Us</h1>

<p>We'd love to hear from you!</p>

<p>
    For technical questions about Zend Framework, please search our 
    <a href="<?php echo $this->basePath() ?>/archives">archives</a>. 
    If your question or issue isn't addressed there, please consider 
    subscribing to one of <a href="http://framework.zend.com/wiki/display/ZFDEV/Mailing+Lists">our mailing lists</a>.
    Your question will be answered much more quickly in one of these forums.
    If you are looking for enterprise Zend Framework support, it is now 
    <a href="http://www.zend.com/en/services/support/zend-framework-support">available online</a>.
</p>

<?php if ($this->error): ?>
<p class="error">
    There were one or more isues with your submission. Please correct them as 
    indicated below.
</p>
<?php endif ?>

<?php 
$form = $this->form;
$form->prepare();
$form->setAttribute('action', $this->url('contact/process'));
$form->setAttribute('method', 'post');
$send = $form->get('Send');
$send->setAttribute('class', 'button');

echo $this->form()->openTag($form);
echo $this->formRow($form->get('from'));
echo $this->formRow($form->get('subject'));
echo $this->formRow($form->get('body'));
echo $this->formRow($form->get('captcha'));
echo $this->formElement($form->get('csrf'));
echo $this->formInput($form->get('Send'));
echo $this->form()->closeTag() ;
?>
<div class="clear"></div>
</section>
