<?php 
$this->layout()->active = 'learn';
$this->layout()->bodyId = 'manual';

// Head title
$this->headTitle()->prepend($this->title);

// Sidebar
$this->render('manual/page-controller/manual/sidebar', array(
    'sidebar'      => $this->sidebar,
    'versions'     => $this->versions,
    'version'      => $this->version,
    'lang'         => $this->lang,
    'contentList'  => $contentList,
    'currentPage'  => $currentPage,
));
?>

<h1 id="component-name"><?php echo $currentPageTitle ?></h1>

<?php
$path = sprintf('manual/%s/%s/', $this->version, $this->lang);
?>
<ul class="breadcrumbs">
    <li><a href="<?php echo $this->url('home') ?>">Home</a></li>
    <li><a href="<?php echo $this->url('learn') ?>">Learn</a></li>
    <li><a href="<?php echo $this->basePath($path . 'index.html') ?>">Reference Guide</a></li>
    <?php if ($currentPageTitle): ?>
        <li><a href="<?php echo $this->basePath($path . $currentPage) ?>"><?php echo $currentPageTitle ?></a></li>
    <?php endif ?>
    <li class="current"><span><?php echo substr($title, 0, strpos($title, ' —')) ?></span></li>
</ul>

<?php
// Body
echo $this->body;

// Discussions
$identifier = 'zfmanual-' . $this->name;
$url        = $this->manualUrl($this->name, $this->lang, $this->version);
echo $this->disqus($identifier, $url, $this->title, 'zfmanual');
?>