<?php // @codingStandardsIgnoreFile
$this->render('page-controller/about/sidebar', array('active' => 'long-term-support'));

$this->headTitle()->prepend('Long Term Support');
$this->headLink()->appendStylesheet('//cdnjs.cloudflare.com/ajax/libs/highlight.js/8.4/styles/default.min.css');
$this->inlineScript()->appendFile('//cdnjs.cloudflare.com/ajax/libs/highlight.js/8.4/highlight.min.js');
?>
<h1>Long Term Support</h1>

<p>
    Periodically, we will announce a Long Term Support (LTS) version. LTS versions are
    supported for a duration of 3 years from the time of release with security and
    critical bug fixes.
</p>

<h2>Adopting an LTS version</h2>

<p>
    Opting-in to an LTS version can be done by modifying the <a 
    href="https://getcomposer.org/">Composer</a> requirement for Zend Framework:
</p>

<pre><code class="lang-console">
$ composer require "zendframework/zendframework:~2.4.0"
</code></pre>

<p>
    The above will modify your <kbd>composer.json</kbd> to pin to the semantic version <kbd>2.4.0</kbd>,
    ensuring you only get updates in the 2.4 series. If you want to pin to a different LTS version,
    specify the <kbd>X.Y.0</kbd> version for it instead.
</p>

<h2>Versions</h2>

<table class="long-term-support">
    <thead>
        <tr>
            <td class="version">Version</td>
            <td class="release">Released</td>
            <td class="lts">LTS Ends</td>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td class="version">Zend Framework 2.4</td>
            <td class="release">2015-03-31</td>
            <td class="lts">2018-03-31</td>
        </tr>
    </tbody>
</table>

<?php $this->inlineScript()->captureStart(); ?>
hljs.initHighlightingOnLoad();
<?php $this->inlineScript()->captureEnd(); ?>
