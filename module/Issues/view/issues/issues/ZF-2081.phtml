<h2>ZF-2081: Zend_Locale_Data creating cacheId fails when cache is set/enable</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2007-10-17T05:03:15.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2007-11-15T14:19:39.000+0000</dd>

    <dt>Status:</dt>
    <dd>Resolved</dd>

    <dt>Fix version(s):</dt>
    <dd><ul>        <li>1.0.3 (30/Nov/07)</li>
    </ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Patrick van Dissel (tdm)
            </dd>

    <dt>Assignee:</dt>
    <dd>
                Thomas Weidner (thomas)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Locale</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>Zend_Locale_Data:</p>

<p>When cache is set/enabled, line 265 gives an error about the implode function that is used.</p>

<p>@version    $Id: Data.php 6356 2007-09-14 19:29:54Z thomas $
Line 265 says:</p>

<pre class="highlight"><code>
$id = 'Zend_Locale_' . $locale . '_' . $path . '_' . implode('_' . $val);
</code></pre>

<p>But the whole implode is unnecesary because it's already done within line 262 and 264.
So line 265 should say:</p>

<pre class="highlight"><code>
$id = 'Zend_Locale_' . $locale . '_' . $path . '_' . $val;
</code></pre>

<p>While making this issue I found another fault, a small miss-typed,
Line 263 says:</p>

<pre class="highlight"><code>
$val = implode('_' . $value);
</code></pre>

<p>Which should say:</p>

<pre class="highlight"><code>
$val = implode('_', $value);
</code></pre>

<p>(note the punctuation mark instead of the dot)</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Thomas Weidner (thomas) on 2007-10-17T05:56:50.000+0000</p> 
        <div class="body">
            <p>Already fixed with SVN 6511.</p>

        </div>
    </div>
    </div>

