<h2>ZF-11581: Only when the whole form is valid, the receive() method should be called and performed</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Sub-task</dd>

    <dt>Created:</dt>
    <dd>2011-07-20T09:35:25.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2011-07-20T09:35:25.000+0000</dd>

    <dt>Status:</dt>
    <dd>Open</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
                etaty (etaty)
            </dd>

    <dt>Assignee:</dt>
    <dd>
                Thomas Weidner (thomas)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_File_Transfer</li>
            <li>Zend_Form</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>receive is never called when the form is valid.</p>

<p>if ($form-&gt;isValid($formData)) {
  if(file_exist($form-&gt;file-&gt;getFileName()) {
    //received has been called
  }
  // received not called
  // some methods to fire the receive()
  // $file = $form-&gt;getValue('file');
  // or $form-&gt;file-&gt;receive();
}</p>

<p>At least please write this in the documentation.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>
    <p class="comments">No comments to display</p>
    
</div>

