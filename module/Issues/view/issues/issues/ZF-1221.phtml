<h2>ZF-1221: Improve Zend_Db_Table_Abstract::fetchRow() docblock for IDE support</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Docs:  Problem</dd>

    <dt>Created:</dt>
    <dd>2007-04-07T15:09:56.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2007-07-05T14:44:03.000+0000</dd>

    <dt>Status:</dt>
    <dd>Resolved</dd>

    <dt>Fix version(s):</dt>
    <dd><ul>        <li>0.9.3 (05/May/07)</li>
    </ul></dd>

    <dt>Reporter:</dt>
    <dd>
        None
                    </dd>

    <dt>Assignee:</dt>
    <dd>
                Bill Karwin (bkarwin)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Db_Table</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>The current docblock for Zend_Db_Table_Abstract::fetchRow() contains:</p>

<pre class="highlight"><code>
* @return Zend_Db_Table_Row_Abstract|null The row results per the
*     Zend_Db_Adapter fetch mode, or null if no row found.
</code></pre>

<p>It would be better for auto-completion within IDEs (well, PDT at least) if it contained:</p>

<pre class="highlight"><code>
* @return Zend_Db_Table_Row_Abstract (or null) The row results per the
*     Zend_Db_Adapter fetch mode, or null if no row found.
</code></pre>

<p>(or similar)</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Bill Karwin (bkarwin) on 2007-04-09T12:50:30.000+0000</p> 
        <div class="body">
            <p>Fixed in revision 4439.</p>

        </div>
    </div>
        <div class="comment">
        <p class="metadata">Posted by Bill Karwin (bkarwin) on 2007-04-13T17:07:41.000+0000</p> 
        <div class="body">
            <p>For what it's worth, the phpdoc manual says that it's okay to list multiple types together in the @param and @return tags, separated by the pip symbol (|).</p>

<p><a href="http://manual.phpdoc.org/HTMLframesConverter/earthli/phpDocumentor/tutorial_tags.return.pkg.html">http://manual.phpdoc.org/HTMLframesConverter/…</a></p>

<p>There is also an instance in the documentation above of a list containing a class name and 'false' which is a value, not a type name:</p>

<pre class="highlight"><code>
/**
 * example of using @return with a class name
 * @param integer even or odd integer
 * @return Parser|false phpDocumentor Parser object or error
 */
</code></pre>

<p>So phpdoc clearly permits (and even encourages) some forms that PDT doesn't understand.</p>

<p>Should we therefore use 'mixed' whenever we have these cases, and then list the possible types and values in the following text, for example:</p>

<pre class="highlight"><code>
* @return mixed Zend_Db_Table_Row_Abstract or null; the row results per the
*     Zend_Db_Adapter fetch mode, or null if no row found.
</code></pre>

<p>In the example above, the PDT won't parse Zend_Db_Table_Row_Abstract at all, it'll just know 'mixed' as the return type in cases like this.</p>

        </div>
    </div>
    </div>

