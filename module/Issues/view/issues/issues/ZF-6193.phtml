<h2>ZF-6193: contextSwitch does not fire for camel cased actions</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2009-04-02T10:19:16.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2009-04-02T10:38:55.000+0000</dd>

    <dt>Status:</dt>
    <dd>Resolved</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Kenrick Buchanan (nsbucky)
            </dd>

    <dt>Assignee:</dt>
    <dd>
                Matthew Weier O'Phinney (matthew)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Controller</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>$ajaxContext = $this-&gt;_helper-&gt;getHelper('AjaxContext');
$ajaxContext-&gt;setAutoJsonSerialization(false)
                    -&gt;addActionContext('fieldSearch', 'json')  // json context will not be called
                    -&gt;addActionContext('getfield', 'json')        // json context will be called
                    -&gt;initContext();</p>

<p>function fieldSearchAction() {...}
function getfieldAction(){}</p>

<p>If I use a camel cased action name with context, it always just asks for the .phtml version, and never the json. Changing the name appears to fix this.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Matthew Weier O'Phinney (matthew) on 2009-04-02T10:38:37.000+0000</p> 
        <div class="body">
            <p>Don't use camelCasing when specifying actions; use the action name as it would be represented in the URL and thus request object: 'field-search'.</p>

        </div>
    </div>
    </div>

