<h2>ZF-12051: Changing default decorators for Zend_Dojo_Form_Element causes setOptions method to be inocuous</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2012-02-09T23:36:57.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-02-09T23:36:57.000+0000</dd>

    <dt>Status:</dt>
    <dd>Open</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
        None
                    </dd>

    <dt>Assignee:</dt>
    <dd>
                Matthew Weier O'Phinney (matthew)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Dojo</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>When trying to change the default decorators for a Zend_Dojo_Form_Element_CheckBox the options set in setOptions method are not taken in consideration.</p>

<p>For instance, with Zend_Form_Element_Checkbox using this code:</p>

<pre class="literal">
$closeonsubmit = new Zend_Form_Element_CheckBox('closeonsubmit');
$closeonsubmit-&gt;setLabel('Fechar após guardar')
  -&gt;setOptions(array('class' =&gt; 'closeonsubmitcheckbox','onClick' =&gt; 'SynchCloseOnSubmit(this)'))
  -&gt;setDecorators(array(array('ViewHelper'),array('HtmlTag', array('tag' =&gt; 'dd', 'class' =&gt; 'closeonsubmit')),
                    array('Label', array('tag'=&gt; 'dt')),));
</pre>

<p>will render:</p>

<pre class="literal">
</pre>
  Fechar após guardar


  


<p>But when using a Dojo element in the same way:</p>

<pre class="literal">
$closeonsubmit = new Zend_Dojo_Form_Element_CheckBox('closeonsubmit');
$closeonsubmit-&gt;setLabel('Fechar após guardar')
  -&gt;setOptions(array('class' =&gt; 'closeonsubmitcheckbox','onClick' =&gt; 'SynchCloseOnSubmit(this)'))
  -&gt;setDecorators(array(array('ViewHelper'),array('HtmlTag', array('tag' =&gt; 'dd', 'class' =&gt; 'closeonsubmit')),
                    array('Label', array('tag'=&gt; 'dt')),));
</pre>

<p>will render:</p>

<pre class="literal">
</pre>
  Fechar após guardar


  


<p>Notice that the onClick event attribute is missing as the class attribute is also.
I don't think that the behaviour should be different between the two.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>
    <p class="comments">No comments to display</p>
    
</div>

