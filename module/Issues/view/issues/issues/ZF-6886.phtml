<h2>ZF-6886: Cannot use dojox.data.QueryReadStore in a Zend_Dojo_Form_Element_FilteringSelect element when using Dojo with a CDN</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2009-06-02T12:41:44.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-08-01T10:56:08.000+0000</dd>

    <dt>Status:</dt>
    <dd>Open</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Nicolas Debarnot (ndebarnot)
            </dd>

    <dt>Assignee:</dt>
    <dd>
                Matthew Weier O'Phinney (matthew)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Dojo</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>I'm using a Zend_Dojo_Form_Element_FilteringSelect with the following parameters :</p>

<p>$oElement-&gt;setStoreId('myStore');
$oElement-&gt;setStoreType('dojox.data.QueryReadStore');</p>

<p>and it ends up with the following javascript error:</p>

<p>dojox.data is undefined</p>

<p>at the following line :</p>

<p>var myStore = new dojox.data.QueryReadStore({"url":"/foo.php"});</p>

<p>if I execute this query in the console, it works fine so it looks like the form is instanciated too early, before the javascripts can be retrieved.</p>

<p>Works fine when using dojo locally and not via cdn.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>
    <p class="comments">No comments to display</p>
    
</div>

