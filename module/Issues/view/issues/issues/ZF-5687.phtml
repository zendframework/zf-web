<h2>ZF-5687: Wrong dispatch in case no route is matching</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2009-02-04T04:59:58.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-11-20T20:52:57.000+0000</dd>

    <dt>Status:</dt>
    <dd>Closed</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Cristian Bichis (avantis)
            </dd>

    <dt>Assignee:</dt>
    <dd>
        None
                    </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Controller</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>Hello,</p>

<p>I am testing some custom routes and i noticed one problem.</p>

<p>On bootstrap i am trying to set app to use just one route:</p>

<p>$router = self::$frontController-&gt;getRouter();
$router-&gt;removeDefaultRoutes();
$router-&gt;addConfig($config, 'routes');</p>

<p>routes.default.type = "Zend_Controller_Router_Route"
routes.default.route = ":seo--1"
routes.default.defaults.module = "default"
routes.default.defaults.controller = "index"
routes.default.defaults.action = "test"</p>

<p>If i am trying to dispatch an URL like that:</p>

<p><a href="www.domains.com/name--1">www.domains.com/name--1</a> all is fine, os it's called the index/test action</p>

<p>But if i try to dispatch just <a href="www.domain.com">www.domain.com</a> the action dispatched is index/index !!!! Even if i removed the default route.</p>

<p>Also, i used this to be sure is fine:</p>

<p>self::$frontController-&gt;setParam('useDefaultControllerAlways', false);</p>

<p>But still is dispatched index/index, instead of an error or something like that.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Rob Allen (rob) on 2012-11-20T20:52:57.000+0000</p> 
        <div class="body">
            <p>Bulk change of all issues last updated before 1st January 2010 as "Won't Fix".</p>

<p>Feel free to re-open and provide a patch if you want to fix this issue.</p>

        </div>
    </div>
    </div>

