<h2>ZF-12140: Zend_Validate NotEmpty types empty_array should be array.</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Docs:  Problem</dd>

    <dt>Created:</dt>
    <dd>2012-04-06T06:52:21.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-04-06T06:52:21.000+0000</dd>

    <dt>Status:</dt>
    <dd>Open</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
        None
                    </dd>

    <dt>Assignee:</dt>
    <dd>
                Thomas Weidner (thomas)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Validate</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p><a href="http://framework.zend.com/manual/en/zend.validate.set.html#zend.validate.set.notempty.types">http://framework.zend.com/manual/en/…</a></p>

<p>empty_array: Returns FALSE when an empty array is given.</p>

<p>should be correct to</p>

<p>array: Returns FALSE when an empty array is given.</p>

<p>by code testing:</p>

<p>when i tried to using</p>

<pre class="highlight"><code>
if (Zend_Validate::is($this-&gt;view-&gt;user, 'NotEmpty', array('empty_array'))) 
</code></pre>

<p>the Zend Framework would halt by errors:</p>

<p>An error occurred
Application error
Exception information:</p>

<p>Message: Unknown type</p>

<p>if i changes the code to :</p>

<pre class="highlight"><code>

if (Zend_Validate::is($this-&gt;view-&gt;user, 'NotEmpty', array('array'))) 
</code></pre>

<p>everything should be fine.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>
    <p class="comments">No comments to display</p>
    
</div>

