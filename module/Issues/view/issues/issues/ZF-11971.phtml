<h2>ZF-11971: Zend DB - PDO conection PGSQL adapter list table error</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2012-01-05T11:46:10.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-04-10T14:09:32.000+0000</dd>

    <dt>Status:</dt>
    <dd>Open</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
        None
                    </dd>

    <dt>Assignee:</dt>
    <dd>
                Ralph Schindler (ralph)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Db</li>
            <li>Zend_Db_Table</li>
            <li>adapter</li>
            <li>db</li>
            <li>pgsql</li>
            <li>postgresql</li>
            <li>schema</li>
            <li>zend_db</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>There was a problem with the list tables with connection with the adapter PDO PGSQL. When I have the same table name in different schemas list just a name not well differentiated by schema.</p>

<p>So I drew up a list SELECT where the tables in the schema PGSQL with differentiation. Below the query mentioned in the function to be tested listTables:</p>

<p>SELECT n.nspname the "schema"
c.relname the "table_name"
C.relkind CASE WHEN 'r' THEN 'table' WHEN 'v' THEN 'view' WHEN 'i' THEN
'index' WHEN 'S' THEN 'sequence' WHEN 's' THEN 'special' END as "typereg"
u.usename the "proprietary"
FROM pg_catalog.pg_class c
INNER JOIN ON u.usesysid pg_catalog.pg_user u = c.relowner
INNER JOIN ON n.oid pg_catalog.pg_namespace n = c.relnamespace
C.relkind WHERE IN ('r','')
And n.nspname NOT IN ('pg_catalog', 'pg_toast', 'information_schema')
ORDER BY 1,2;</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Adam Lundrigan (adamlundrigan) on 2012-02-26T06:36:07.000+0000</p> 
        <div class="body">
            <p>Could you please provide more information on what you are trying to accomplish?</p>

        </div>
    </div>
        <div class="comment">
        <p class="metadata">Posted by Fabricio Valadares (fsvxavier) on 2012-04-10T14:09:32.000+0000</p> 
        <div class="body">
            <p>I'm generating the models and when I search for some tables in postgresql schema did not return the table schema if there is a table with the same name in another schema.</p>

        </div>
    </div>
    </div>

