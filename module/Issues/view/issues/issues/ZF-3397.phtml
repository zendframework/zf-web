<h2>ZF-3397: gd:extendedProperty spec change: XML blob inclusion</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Improvement</dd>

    <dt>Created:</dt>
    <dd>2008-06-05T10:38:48.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-11-20T20:53:27.000+0000</dd>

    <dt>Status:</dt>
    <dd>Closed</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
        None
                    </dd>

    <dt>Assignee:</dt>
    <dd>
        None
                    </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Gdata</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>Currently Zend_Gdata_Extension_ExtendedProperty implements data storage by using key and value attributes. This appears to be consistent with Google's previous documentation</p>

<p>However, Google's new Contacts Data API docs seem to be changing things.</p>

<p>bq.  Value of the extended property may be stored as a value (arbitrary string) <em>or</em> an XML blob (these are mutually exclusive; instead of a valid XML blob plain text can be used).
<a href="http://code.google.com/apis/contacts/reference.html#gd:extendedProperty">http://code.google.com/apis/contacts/…</a></p>

<p>The examples given are</p>

<pre class="highlight"><code>
text value</code></pre>

<p>Currently the class only supports the first case.</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Darien Hager (hagerd) on 2008-08-12T12:21:40.000+0000</p> 
        <div class="body">
            <p>Proof of concept w/o namespace stuff:</p>

<pre class="highlight"><code>
&lt;?php
$doc = new DOMDocument();
$xmlBlob = "text";


$frag = $doc-&gt;createDocumentFragment();
$extended = new DOMElement("extendedProperty");
$doc-&gt;appendChild($extended);

$extended-&gt;setAttribute("name","SomePropertyName");
$frag-&gt;appendXML($xmlBlob);
$extended-&gt;appendChild($frag);

echo $doc-&gt;saveXML(); 
?&gt;</code></pre>

<p>Output is:</p>

<pre class="highlight"><code>
&lt;?xml version="1.0"?&gt;
text</code></pre>

        </div>
    </div>
        <div class="comment">
        <p class="metadata">Posted by Rob Allen (rob) on 2012-11-20T20:53:27.000+0000</p> 
        <div class="body">
            <p>Bulk change of all issues last updated before 1st January 2010 as "Won't Fix".</p>

<p>Feel free to re-open and provide a patch if you want to fix this issue.</p>

        </div>
    </div>
    </div>

