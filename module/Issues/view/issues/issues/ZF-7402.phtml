<h2>ZF-7402: Segmentation Fault when sending long messages with Zend_Rest_Client</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Bug</dd>

    <dt>Created:</dt>
    <dd>2009-07-27T23:34:57.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2012-11-20T20:52:33.000+0000</dd>

    <dt>Status:</dt>
    <dd>Closed</dd>

    <dt>Fix version(s):</dt>
    <dd><ul></ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Søren Haagerup (shaagerup)
            </dd>

    <dt>Assignee:</dt>
    <dd>
        None
                    </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Rest_Client</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <pre class="highlight"><code>
&lt;?php

ini_set('include_path', ini_get('include_path') . PATH_SEPARATOR . 'workspace/library');
require_once('Zend/Rest/Client.php');

function getLongString($length)
{
    $str = '';
    for ($i=0; $i&lt;$length; $i++)
        $str .= 'a';
    return $str;
}

$client = new Zend_Rest_Client('<a href="http://localhost/">http://localhost/</a>');
$url = $client-&gt;testFunc(getLongString((int)$argv[1]))-&gt;get();  
echo "Terminated correctly with length ".(int)$argv[1]."\n";
?&gt;
</code></pre>

<p>Testing this</p>

<pre class="literal">
$ php testRest.php 3000
Terminated correctly with length 3000
$ php testRest.php 4000
Segmentation fault
</pre>

<p>I run into this, when sending large chunks of XML through an REST API. Maybe this was not the intention -- are there recommended ways to transfer XML data to a REST Server?</p>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Rob Allen (rob) on 2012-11-20T20:52:33.000+0000</p> 
        <div class="body">
            <p>Bulk change of all issues last updated before 1st January 2010 as "Won't Fix".</p>

<p>Feel free to re-open and provide a patch if you want to fix this issue.</p>

        </div>
    </div>
    </div>

