<h2>ZF-2511: Zend_Cache load strict operator comparison</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
    <dd>Docs:  Improvement</dd>

    <dt>Created:</dt>
    <dd>2008-01-28T09:52:45.000+0000</dd>

    <dt>Last Updated:</dt>
    <dd>2008-09-02T10:39:18.000+0000</dd>

    <dt>Status:</dt>
    <dd>Resolved</dd>

    <dt>Fix version(s):</dt>
    <dd><ul>        <li>1.6.0 (02/Sep/08)</li>
    </ul></dd>

    <dt>Reporter:</dt>
    <dd>
                Andries Seutens (andries)
            </dd>

    <dt>Assignee:</dt>
    <dd>
                Fabien MARTY (fab)
            </dd>

    <dt>Tags:</dt>
    <dd><ul>        <li>Zend_Cache</li>
        </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    </ul></dd>
</dl>

<div class="description">
    <h3>Description</h3>

    <div class="body">
        <p>The code snippets in the Zend_Cache docs don't use the strict operator, thus an empty cache-object behaves like a cache-miss:</p>

<pre class="highlight"><code>
if (!($data = $cache-&gt;load($cacheId))) {
    // cache miss...
}
</code></pre>

<p>So if the loaded cache is just empty in a file-cache it looks like: " -1571559392a:0:{}" it will be generated again.</p>

<p>Docs should use the strict comparison:</p>

<pre class="highlight"><code>
if (false === ($data = $cache-&gt;load($cacheId))) {
    // cache miss
}
</code></pre>

    </div>
</div>

<div class="comments">
    <h3>Comments</h3>

    <div class="comment">
        <p class="metadata">Posted by Fabien MARTY (fab) on 2008-06-20T12:53:36.000+0000</p> 
        <div class="body">
            <p>fixed in SVN trunk (thanks)</p>

        </div>
    </div>
        <div class="comment">
        <p class="metadata">Posted by Wil Sinclair (wil) on 2008-09-02T10:39:18.000+0000</p> 
        <div class="body">
            <p>Updating for the 1.6.0 release.</p>

        </div>
    </div>
    </div>

