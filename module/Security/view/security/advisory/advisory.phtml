<?php
$this->render('security/sidebar', array('active' => 'advisories'));

$this->headTitle()->prepend('Security');
$this->headTitle()->prepend('Advisories');
$this->headTitle()->prepend($advisory['title']);

$id = $this->advisory['id'];
if (!$this->resolver()->resolve('security/advisory/' . $id)) {
    $this->error = 'Invalid advisory identifier';
}
?>
<h1>Security Advisory</h1>

<?php if ($this->error): ?>
    <p><?php echo $this->error ?></p>
<?php else: ?>
    <?php echo $this->render('security/advisory/' . $id) ?>
<?php endif ?>

<p class="date">
    Released <?php echo $this->advisory['date'] ?>.
</p>

<p class="back">
   <a href="<?php echo $this->url('security/default', array('action' => 'advisories')) ?>">
       back to advisories
   </a>
</p>
