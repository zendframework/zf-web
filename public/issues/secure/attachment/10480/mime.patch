@@ -143,7 +143,7 @@
             }
 
             // Check if there is a space at the end of the line and rewind
-            if ($str[$ptr - 1] == ' ') {
+            if ($str[$ptr - 1] == ' ' && $ptr !== 1) {
                 --$ptr;
             }
 
