<?php

foreach ($this->contracts as $contract) {
	// prints contract details
}

$params = array(
	'url'			=> $this->baseUrl() . '/contracts/search' . '/id/' . $this->search_id,
	'page_prefix'	=> '/page/',
);
print $this->paginationControl($this->contracts, PAGINATOR_STYLE, '_pagination.phtml', $params);