Index: documentation/manual/en/module_specs/Zend_Controller-Router-Route.xml
===================================================================
--- documentation/manual/en/module_specs/Zend_Controller-Router-Route.xml	(revision 19290)
+++ documentation/manual/en/module_specs/Zend_Controller-Router-Route.xml	(working copy)
@@ -310,7 +310,7 @@
 $route->assemble(array());
 
 // Assemble the URL in german: archiv
-$route->assemble(array());
+$route->assemble(array('@locale' => 'de'));
 ]]></programlisting>
 
         <para>
@@ -333,7 +333,7 @@
 $route->assemble(array('controller' => 'archive', 'foo' => 'bar'));
 
 // Assemble the URL in german: archiv/uebersicht/foo/bar
-$route->assemble(array('controller' => 'archive', 'foo' => 'bar'));
+$route->assemble(array('controller' => 'archive', 'foo' => 'bar', '@locale' => 'de'));
 ]]></programlisting>
 
         <para>
