<h2>{{key}}: {{summary}}</h2>

<dl class="metadata">
    <dt>Issue Type:</dt>
{{! If a Sub-task, need to link it to parent }}
{{! Parent info is in "parent" reference -- see ZF2-554 for an example.}}
{{! There are different directions -- inbound, outbound, etc; check out definitions. }}

    <dd>{{issuetype}}</dd>

    <dt>Status:</dt>
    <dd>{{status}}</dd>

    <dt>Fix version(s):</dt>
    <dd><ul>{{#fix_versions}}
    {{%IMPLICIT-ITERATOR%}}
        <li>{{.}}</li>
    {{/fix_versions}}</ul></dd>

    <dt>Reporter:</dt>
    <dd>{{reporter.name}} ({{reporter.nick}})</dd>

    <dt>Created:</dt>
    <dd>{{created}}</dd>

    <dt>Last Updated:</dt>
    <dd>{{updated}}</dd>

    <dt>Assignee:</dt>
    <dd>{{assignee.name}} ({{assignee.nick}})</dd>

    <dt>Tags:</dt>
    <dd><ul>{{#tags}}
    {{%IMPLICIT-ITERATOR%}}
        <li>{{.}}</li>
    {{/tags}}
    </ul></dd>

    <dt>Related issues:</dt>
    <dd><ul>
    {{#related}}
    {{%IMPLICIT-ITERATOR%}}
        <li><a href="/issues/browse/{{.}}">{{.}}</a></li>
    {{/related}}
    </ul></dd>

    <dt>Attachments:</dt>
    <dd><ul>
    {{#attachments}}
        <li><a href="{{url}}">{{filename}}</a></li>
    {{/attachments}}
    </ul></dd>
</dl>


<div class="description">
    <h3>Description</h3>

    {{{description}}}
</div>

<div class="comments">
    <h3>Comments</h3>

    {{^comments}}
    <p class="comments">No comments to display</p>
    {{/comments}}

    {{#comments}}
    <div class="comment">
        <p class="metadata">Posted by {{author.displayName}} ({{author.name}}) on {{created}}</p> 
        <div class="body">
            {{{body}}}
        </div>
    </div>
    {{/comments}}
</div>

